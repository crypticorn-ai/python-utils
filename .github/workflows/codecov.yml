---
# https://github.com/marketplace/actions/pytest-coverage-comment
name: Test and Coverage

on: [push, pull_request]

permissions:
  contents: write
  checks: write
  pull-requests: write

env:
  API_ENV: ${{ (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') && 'prod' || 'dev' }}
  JWT_SECRET: ${{ secrets.JWT_SECRET }}

jobs:
  ci:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13", "3.14"]

    steps:
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Check-out repository
        uses: actions/checkout@v4

      - name: Generate .env
        run: ./scripts/merge-env.sh

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          version: "0.9.17"

      - name: Install the project
        run: uv sync --locked --dev

      - name: Test with pytest
        run: uv run pytest tests/ --cov=crypticorn_utils --cov-branch --cov-report=xml --cov-report=term-missing:skip-covered

      - name: Pytest coverage comment
        if: ${{ matrix.python-version == '3.14' }} # current docker python backend version
        uses: MishaKav/pytest-coverage-comment@main
        with:
          title: Coverage Report ${{ matrix.python-version }}
          pytest-xml-coverage-path: ./coverage.xml
          report-only-changed-files: true
          hide-report: true
